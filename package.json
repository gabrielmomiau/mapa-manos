{
  "name": "mapa-manos",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/preparar-modelos.mjs",
    "dev": "vite --configLoader runner",
    "prebuild": "node scripts/preparar-modelos.mjs",
    "build": "vite --configLoader runner build",
    "preview": "vite --configLoader runner preview",
    "test": "node --test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35"
  },
  "devDependencies": {
    "vite": "7.3.6"
  }
}
